akka {
  loglevel = "DEBUG"
  actor {
    debug {
      receive = on
      autoreceive = off
      lifecycle = off
    }
  }
}
spray {
    can {
        host-connector {
            max-redirects = 10
        }
    }
}