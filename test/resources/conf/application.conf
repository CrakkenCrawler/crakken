akka {
  loglevel = "WARNING"
  actor {
    debug {
      receive = off
      autoreceive = off
      lifecycle = off
    }
  }
}

mongodb.db = "crakken"
application.global = crakken.test.SimpleGlobal

