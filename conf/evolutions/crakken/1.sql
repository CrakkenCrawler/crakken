# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "CRAWL_REQUESTS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,"ORIGIN" VARCHAR NOT NULL,"INITIAL_RECURSION_LEVEL" INTEGER NOT NULL,"INCLUDE_EXTERNALS" BOOLEAN NOT NULL);
create table "PAGE_FETCH_REQUESTS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,"CRAWLREQUEST_ID" BIGINT,"URL" VARCHAR NOT NULL,"STATUS_CODE" INTEGER,"CONTENT" VARCHAR,"RECURSION_LEVEL" INTEGER NOT NULL,"INCLUDE_EXTERNALS" BOOLEAN NOT NULL);

# --- !Downs

drop table "CRAWL_REQUESTS";
drop table "PAGE_FETCH_REQUESTS";

